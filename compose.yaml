services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    volumes:
      - .:/workspace
    ports:
      - "6380:6380"
    init: true
    stdin_open: true
    tty: true
    command: ["sleep", "infinity"]

